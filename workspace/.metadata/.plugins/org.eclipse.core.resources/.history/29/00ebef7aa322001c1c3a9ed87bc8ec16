package it.capgemini.academy.giorno4.model;

public interface EsempioInterfaccia {
	// è un'unità di compilazione che contiene solo metodi astratti
	// si usa per uniformare parte del comportamento di classi appartenenti a gerarchie differenti
	// una classe può implementare più interfacce
}
